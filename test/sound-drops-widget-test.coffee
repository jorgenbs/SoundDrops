chai = require 'chai'
assert = chai.assert
soundDrops = require '../public/js/sound-drop'
#socketFake = require './fakes/socketFake'
#$ = require './fakes/jqueryFake'

#describe 'widget positioning', ->
  #it 'x-pos should always be less than minimum', ->
    #sd = soundDrops $ socketFake
    #assert(soundDrops.SoundDrop._randx() > 0)